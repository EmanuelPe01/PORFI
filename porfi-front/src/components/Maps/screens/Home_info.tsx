import { BtnMyLocation, MapView, ReactLogo, BtnSeeRoute} from "../components"

export const HomeInfo = ( ) => {
    
    return(
        <div>
            <MapView/>
            <BtnSeeRoute/>
            <ReactLogo/>
        </div>
    )
}